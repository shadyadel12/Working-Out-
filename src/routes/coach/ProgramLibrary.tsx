import { useState } from 'react';import{useMutation,useQuery,useQueryClient}from'@tanstack/react-query';import{useAuth}from'../../auth/AuthContext';import{createProgramTemplate,deleteProgramTemplate,duplicateProgramTemplate,listProgramTemplates,updateProgramTemplate,type ProgramTemplate}from'../../api/programTemplates';
export default function ProgramLibrary(){const{session}=useAuth();const coachId=session!.user.id;const qc=useQueryClient();const[search,setSearch]=useState('');const[open,setOpen]=useState(false);const[editing,setEditing]=useState<ProgramTemplate|null>(null);const[name,setName]=useState('');const[description,setDescription]=useState('');const[difficulty,setDifficulty]=useState('Beginner');const[duration,setDuration]=useState(1);const query=useQuery({queryKey:['program-templates',coachId],queryFn:()=>listProgramTemplates(coachId)});const refresh=()=>qc.invalidateQueries({queryKey:['program-templates',coachId]});const save=useMutation({mutationFn:()=>{const input={name:name.trim(),description:description.trim()||null,difficulty,duration_weeks:duration};return editing?updateProgramTemplate(editing.id,input):createProgramTemplate(coachId,input)},onSuccess:async()=>{await refresh();close()}});const duplicate=useMutation({mutationFn:duplicateProgramTemplate,onSuccess:refresh});const remove=useMutation({mutationFn:deleteProgramTemplate,onSuccess:refresh});const rows=(query.data??[]).filter(x=>x.name.toLowerCase().includes(search.toLowerCase()));function show(item?:ProgramTemplate){setEditing(item??null);setName(item?.name??'');setDescription(item?.description??'');setDifficulty(item?.difficulty??'Beginner');setDuration(item?.duration_weeks??1);setOpen(true)}function close(){setOpen(false);setEditing(null);setName('');setDescription('')}return <div className="exercise-library-page"><div className="library-heading"><h1>Program Library</h1><button onClick={()=>show()}>+ Create Program</button></div><div className="library-table-card"><div className="clients-search"><span>⌕</span><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search by program name…"/></div><div className="clients-table-scroll"><table className="clients-table library-table"><thead><tr><th>Name</th><th>Difficulty</th><th>Duration</th><th>Created</th><th></th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td><strong>{x.name}</strong></td><td>{x.difficulty}</td><td>{x.duration_weeks} week{x.duration_weeks===1?'':'s'}</td><td>{new Date(x.created_at).toLocaleDateString()}</td><td className="library-actions"><button className="secondary" onClick={()=>show(x)}>Edit</button><button className="secondary" onClick={()=>duplicate.mutate(x)}>Copy</button><button className="danger" onClick={()=>confirm(`Delete ${x.name}?`)&&remove.mutate(x.id)}>Delete</button></td></tr>)}</tbody></table></div>{!rows.length&&<div className="library-empty"><strong>No Programs Found</strong><span>Create your first reusable program.</span></div>}</div>{open&&<div className="workout-modal-backdrop"><section className="workout-modal program-library-modal"><header><h2>{editing?'Edit Program':'Create Program'}</h2><button className="modal-close" onClick={close}>×</button></header><div className="workout-modal-body"><div className="field"><label>Program Name</label><input autoFocus value={name} onChange={e=>setName(e.target.value)}/></div><div className="field"><label>Description (Optional)</label><textarea rows={3} value={description} onChange={e=>setDescription(e.target.value)} placeholder="Add a description for this program"/></div><div className="field"><label>Difficulty Level</label><select value={difficulty} onChange={e=>setDifficulty(e.target.value)}><option>Beginner</option><option>Intermediate</option><option>Advanced</option></select></div><div className="field"><label>Duration (Weeks)</label><input type="number" min="1" max="104" value={duration} onChange={e=>setDuration(Number(e.target.value))}/></div>{save.error&&<p className="error">{(save.error as Error).message}</p>}</div><footer><button className="secondary" onClick={close}>Cancel</button><button disabled={!name.trim()||save.isPending} onClick={()=>save.mutate()}>{save.isPending?'Saving…':editing?'Save Changes':'Create Program'}</button></footer></section></div>}</div>}
