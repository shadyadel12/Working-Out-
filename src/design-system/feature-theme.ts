export const featureTheme = {
  clients: { color: 'var(--blue)', soft: 'var(--blue-soft)' },
  training: { color: 'var(--secondary)', soft: 'var(--secondary-soft)' },
  nutrition: { color: 'var(--green)', soft: 'var(--green-soft)' },
  messages: { color: 'var(--pink)', soft: 'var(--pink-soft)' },
  analytics: { color: 'var(--primary)', soft: 'var(--primary-soft)' },
  resources: { color: 'var(--orange)', soft: 'var(--orange-soft)' },
} as const;
