const rules:Record<string,{kind:'image'|'video'|'audio';max:number}>={'image/jpeg':{kind:'image',max:10},'image/png':{kind:'image',max:10},'image/webp':{kind:'image',max:10},'image/gif':{kind:'image',max:10},'video/mp4':{kind:'video',max:50},'video/webm':{kind:'video',max:50},'video/quicktime':{kind:'video',max:50},'audio/mpeg':{kind:'audio',max:25},'audio/mp4':{kind:'audio',max:25},'audio/wav':{kind:'audio',max:25},'audio/ogg':{kind:'audio',max:25},'audio/webm':{kind:'audio',max:25}};
export function validateMedia(bytes:ArrayBuffer,mime:string,size:number,expected:'image'|'video'|'audio',maxMb?:number){const rule=rules[mime];if(!rule||rule.kind!==expected)throw new Error('Only supported picture, video, and audio formats are allowed.');const limit=maxMb??rule.max;if(size<=0||size>limit*1024*1024)throw new Error(`${expected} must be smaller than ${limit} MB.`);const b=new Uint8Array(bytes.slice(0,16)),a=String.fromCharCode(...b);const ftyp=a.slice(4,8)==='ftyp',webm=b[0]===0x1a&&b[1]===0x45&&b[2]===0xdf&&b[3]===0xa3;const ok:Record<string,boolean>={'image/jpeg':b[0]===0xff&&b[1]===0xd8&&b[2]===0xff,'image/png':b[0]===0x89&&a.slice(1,4)==='PNG','image/gif':a.startsWith('GIF87a')||a.startsWith('GIF89a'),'image/webp':a.startsWith('RIFF')&&a.slice(8,12)==='WEBP','video/mp4':ftyp&&a.slice(8,12)!=='qt  ','video/quicktime':ftyp&&a.slice(8,12)==='qt  ','video/webm':webm,'audio/mpeg':a.startsWith('ID3')||(b[0]===0xff&&(b[1]&0xe0)===0xe0),'audio/mp4':ftyp,'audio/wav':a.startsWith('RIFF')&&a.slice(8,12)==='WAVE','audio/ogg':a.startsWith('OggS'),'audio/webm':webm};if(!ok[mime])throw new Error('The selected file content does not match its reported type.');}
