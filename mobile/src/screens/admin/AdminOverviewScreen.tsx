import { useEffect,useState } from 'react';import { ActivityIndicator,StyleSheet,Text,View } from 'react-native';import { Card,Screen,textStyles } from '../../components/Screen';import { supabase } from '../../lib/supabase';import { colors,radius } from '../../theme';
export default function AdminOverviewScreen(){const[data,setData]=useState<any>(null);async function load(){const[p,l,k,m]=await Promise.all([supabase.from('profiles').select('role'),supabase.from('coach_player_links').select('status,player_id'),supabase.from('coach_keys').select('status'),supabase.from('admin_messages').select('coach_id')]);setData({coaches:(p.data??[]).filter(x=>x.role==='coach').length,players:(p.data??[]).filter(x=>x.role==='player').length,active:(l.data??[]).filter(x=>x.status==='active'&&x.player_id).length,keys:(k.data??[]).filter(x=>x.status==='active').length,support:new Set((m.data??[]).map(x=>x.coach_id)).size})}useEffect(()=>{void load()},[]);return <Screen title="Admin Overview" subtitle="Users, access, and support activity">{!data?<ActivityIndicator color={colors.accent}/>:<><View style={styles.grid}><Metric value={data.coaches} label="Coaches"/><Metric value={data.players} label="Players"/><Metric value={data.active} label="Active access"/><Metric value={data.keys} label="Coach keys"/></View><Card><Text style={textStyles.eyebrow}>Support</Text><Text style={styles.support}>{data.support}</Text><Text style={textStyles.heading}>Coach conversations</Text><Text style={textStyles.muted}>Use Support to review and answer coaching-team requests.</Text></Card></>}</Screen>};function Metric({value,label}:{value:number;label:string}){return <View style={styles.metric}><Text style={styles.value}>{value}</Text><Text style={textStyles.muted}>{label}</Text></View>}const styles=StyleSheet.create({grid:{flexDirection:'row',flexWrap:'wrap',gap:10},metric:{width:'48%',backgroundColor:colors.surface,borderColor:colors.border,borderWidth:1,borderRadius:radius.lg,padding:15},value:{color:colors.text,fontSize:29,fontWeight:'900'},support:{color:colors.accent,fontSize:38,fontWeight:'900'}});
