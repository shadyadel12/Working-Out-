import { Text } from 'react-native';import { Card,Screen,textStyles } from '../components/Screen';import { Button } from '../components/Controls';
export function TermsScreen({back}:{back:()=>void}){const year=new Date().getFullYear();const sections=[['1. Ownership & Copyright',`All content, design, code, graphics, and features are the property of Coach Platform. © ${year} Coach Platform.`],['2. Prohibited Actions','Do not copy, redistribute, reverse-engineer, scrape, resell, or sublicense the platform without written permission.'],['3. User Data','Workout programs, logs, and personal information belong to their respective users. We do not sell user data.'],['4. Accounts & Access','Access requires a valid key. Sharing, transferring, or selling accounts or keys is prohibited.'],['5. Disclaimer','The platform supports fitness coaching and information. Consult a qualified medical professional before starting exercise.'],['6. Changes','Continued use after terms are updated means the updated terms are accepted.'],['7. Contact','Contact your coach or platform administrator for inquiries.']];return <Screen title="Terms of Use" subtitle={`Last updated: July ${year}`}><Button secondary onPress={back}>← BACK</Button>{sections.map(([h,p])=><Card key={h}><Text style={textStyles.heading}>{h}</Text><Text style={textStyles.body}>{p}</Text></Card>)}</Screen>}
export function UpdatesScreen({back}:{back:()=>void}){const groups:[string,string[]][]=[['For Coaches',['Manage players, subscriptions, VIP priority and scheduled check-ups.','Build and reuse workout, diet and program libraries.','Review workout and diet progress, notes, goals and submitted videos.','Use private player chat and administrator support.']],['For Players',['Follow guided set-by-set workouts and meal-by-meal diet plans.','Record reps, optional weight, comments and safely scanned videos.','Filter workout and diet progress and update the full sports profile.','Send text, pictures, videos, audio and voice messages.']],['For Administrators',['Manage coaches, invitations, player access, renewals and revocation.','Use extra account verification and answer coach support.']],['Protection',['Role-based database rules remain the final access boundary.','Uploads are checked for size, type and actual content.','Sessions use protected device storage and expired access is blocked.']]];return <Screen title="Features & Updates" subtitle="Mobile feature overview · July 2026"><Button secondary onPress={back}>← BACK</Button>{groups.map(([h,items])=><Card key={h}><Text style={textStyles.heading}>{h}</Text>{items.map(x=><Text key={x} style={textStyles.body}>• {x}</Text>)}</Card>)}</Screen>}
